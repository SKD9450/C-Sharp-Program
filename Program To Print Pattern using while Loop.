using System;
class Pattern
{
    static void Main()
    {
        int ctr = 1;
        while (ctr <= 5)
        {
            int ptr = 5;
            while (ptr >= ctr)
            {
                Console.Write(ctr);
                ptr--;
            }
            Console.WriteLine();
            ctr++;
            
        }
    }
}
